name: Create Releaseon:push:tags:- "v*"jobs:release:runs-on: ubuntu-lateststeps:- name: Checkout Repouses: actions/checkout@v3      - name: Create ZIP        run: |          zip -r hundesystem.zip custom_components/ README.md hacs.json      - name: Upload Release        uses: softprops/action-gh-release@v1        with:          files: hundesystem.zip